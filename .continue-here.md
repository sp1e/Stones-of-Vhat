---
project: Stones of Vhat / Game1
phase: B02a live Slicer controller and contact view
status: in_progress_live_slicer
updated: 2026-09-13
---

# Current continuation

## Active development — 2026-09-13, automatic continuation requested

Latest 12:30 UTC state supersedes the chronological progress below: Live Slicer is **accepted locally** after final independent SPEC PASS then fresh QUALITY APPROVE. Parent strict/native **202/202 PASS (162582.1267 ms)**, full browser **22/22 PASS (149261.0724 ms)**, production build PASS and all six final captures opened. Frozen-source physical phase probe reproduced 80 scenarios / 76 hits / 4 expiries. Full evidence/limits: `2026-09-13-live-slicer-results.md`. 4178 is closed; parent preview 5173 is separate. Source is being scoped for commit/publication; verify the actual next SHA rather than assuming it is still dependency `fcd186e`.

Simon reports Claude is back after token exhaustion. Fresh read-only environment HEAD is `cf9a87711a61066d33aa0fd11c4465556c95cded`, with a new tree fix and preserved WIP. Old paused restart-point is historical; no Codex process restart/merge or Claude-file edit occurred. New user-requested instructions: `docs/handoffs/2026-09-13-claude-resume-environment.md`, also linked from `claude-sync.md`. The current chained escape sweep, terminal RAF and mutation-evidence gaps were reread and remain assigned to Claude; no acknowledgment yet.

No second production writer. Next authored-figure plan is still a draft: wide disappearing elbow geometry was rejected as gameplay severing. Preferred feasibility path is pre-authored seam halves/caps pose-baked at the captured pose into their own components, retaining the exterior and original inverse binds; exact data, shader-normal preservation and independent contract review remain before authoring. No asset generation/install. Full player/NPC injury, sword, gore and humanoid ragdoll are still required downstream.

Simon requests continuous development until stop, no routine user approval checkpoints, and periodic Claude synchronization. A thread heartbeat `stones-of-vhat-utveckling-och-claude-synk` runs every 30 minutes with explicit stop/pause and single-writer safeguards. It must not restart development after a later pause request. Independent tests/reviews remain required.

User also explicitly reaffirmed raw magic/sword FPS action and symmetric regional injuries/limb loss for player and NPC; read `docs/superpowers/specs/2026-09-13-combat-injury-addendum.md`. Do not replace the combat pillar with mystery-only exploration.

Owner batch is accepted and published as **fcd186e19947a5144624815ea11f79036ffe9d6c**: final independent SPEC PASS then fresh QUALITY APPROVE. Both original null-allowance/inherited-sparse-dimension defects fixed RED→GREEN; real native transaction controls retained. Parent34/34 focused5554.287ms,183/183 strict/native45323.785ms, build/staged whitespace PASS. All4 actual blockers, once-only owner capture/compilation, shared512 reserve/refund and no replay verified. Results: `2026-09-13-owned-contact-batch-results.md`. Live sp1e/canonical-origin/fetched ancestry checks preceded atomic push; both main/runtime verified at that SHA. Only the user research cache remained untracked afterwards.

The current executable next task is the nine-path `2026-09-13-live-slicer-plan.md`, independent contract PASS, with its companion design fully reviewed. It includes a retained fixed-tick controller, actual default16m/s boundary-born horizontal/vertical blade, bounded events, pure contact frame and PC controls/visual inspection. No live controller, damage or visible Slicer acceptance exists yet. Parent retained/reviewed the supplemental24-case `game/scripts/probe-boundary-slicer.mjs` and reran corrected frozen source:24/24hits,24worlds destroyed, owner unchanged. This script is pair/family orchestration, not the owner-budget test or visible acceptance.

Fresh Sol/high `live_slicer_impl` is the sole nine-path writer, starting with full skill/plan/source reads then sequential RED→GREEN controller, contact-frame, UI/browser tasks. Parent owns docs/Git/full suites and separate read-only coordination. No competing production writer, nested worker teams, packages, Windows/security actions or Claude environment edits. Await source-ready for independent SPEC then fresh QUALITY; do not run a conflicting4178 browser server while its owned arm suite runs. Parent preview5173 remains separate.

At approximately12:00UTC, writer declared all nine paths **source-ready/frozen**: strict/focusednative PASS,12/12armbrowser PASS, finalfocusCSS3relevant PASS,4178closed. Fresh Astra/high `live_slicer_spec` now owns read-only implementation SPEC. Initial required post-step atomic-failure coverage/test-loop and stale cancellation-label issues are being consolidated; do not mark reviewed or resume edits before its packet. Parent final suites/QUALITY/publication are pending. Current running results: `2026-09-13-live-slicer-results.md`.

Live writer progress11:20UTC: controller missing-module RED→first9/9 native GREEN including24boundary cases and30/60/144Hz+8stepcatchup; pure contact-frame missing-helper RED→6/6 GREEN/typecheck; browser missing `#cast-slicer` RED observed before UI implementation, which is now active. These are writer progress, not final reviewed acceptance. Parent read the controller/frame sources and requested retained canonical cast/projectile IDs in cadence comparison, the precise upper-arm-blocking X negative, independent full transform/rotation checks and the async-loading/context-loss recovery control before source-ready. The render path must avoid cloning16full retained traces each idle frame.

Progress11:47UTC: those native coverage corrections are present in the working source; final runs/reviews remain open. Parent read current UI/view/HTML/CSS and found a contact-inspector escape lock, now covered by actual controls in writer tests. Parent separately drove its5173preview with Playwright click→real forearm hit→paused contact, opened `.playtest/parent-slicer-contact-wip.png`, and observed rendered blade/path/two witnesses/normal, GPU19geometries/1texture/7programs at1440×900. This is a WIP smoke, not final source/visual acceptance. Writer is still the sole nine-path production owner and extending browser lifecycle/resource coverage; no independent implementation SPEC/QUALITY started yet.

The positive browser physical cast now uses actual UI handlers at handoff/birth tick30 and yields a genuine forearm hit. A separate tick57,physicalAge0,horizontal16m/s case once yielded forearm `inconclusive/width` and later runs hit. Cause is **not established**; do not label native nondeterminism as proven or replace the30-positive with a broad hit-or-uncertain pass. Parent's supplemental `game/scripts/probe-physical-slicer-launches.mjs` on current WIP surveyed80fresh scenarios (handoff1/30/57/90;physicalAge0/1/4/12/30;16/40;H/V):76hit,4expired(allage12/16/H), allage0positive including57,80teardownblocks. Its initial reporting draft misread frontier refs as candidate objects and failed; corrected80case run exited0. Calls in its rows are terminal-family work, not cumulative-flight work. Independent diagnostic review is pending; this is not a native regression, hardware/FPS or geometric certification of all misses.

Parent-only next-stage preparation is saved in `2026-09-13-anatomy-asset-next-slice.md` and `2026-09-13-installed-character-export.md`. web-3d-asset-pipeline skill influenced the DCC→GLB/skin/cap/LOD/instance validation boundary. Read-only Blender5.2.1LTS/exporter5.2.40 inventory confirms a feasible local export path, missing local glTF Transform/validator tools and actual weight truncation/naming hazards. No asset/export/install done. Desktop policy currently excludes GLB/audio/KTX2; future narrow B07 tests must drive changes, no policy/Defender action now. These documents are working preparation, not another production writer or executable asset plan.

New `2026-09-13-elbow-skin-seam-options.md` retains independent read-only skin/cloth seam analysis and parent's finite-width cap-closure qualification. Proposed intact-only deformation bridge plus component-owned caps needs an exact authored/export/runtime test plan; it is not an exported figure or implemented cut. Registry-only checks of official glTF Transform/Khronos tools are in the exporter inventory, with versions/integrities and no installation. No second source writer.

Family resolver is accepted and published as **5d7e56d9a1370ebdf5f3f604b2e6951cbb626cd5** after final independent SPEC PASS and fresh Astra/high QUALITY APPROVE. Parent final12/12 focused568.601ms,167/167 strict/native PASS34.594s, production build and whitespace PASS. Full real-source/manifest/result permutation and identity/kind/reason test omissions closed without production changes. Complete-arm free Z muzzle selects forearm; horizontal muzzle inside upper-arm correctly blocks0. Evidence: `2026-09-13-contact-family-results.md`.

Pair query is now **accepted and published as74d8f7a0e8a78a6476eb12af1e2ddd9df0218a6d**: all initial SPEC findings closed through retained tests, final SPEC PASS then fresh QUALITY APPROVE. Parent final18/18 focused4502.436ms,155/155 strict/native PASS35.452s, production build and staged whitespace PASS. Exact evidence and corrected CPU benchmark are in `2026-09-13-contact-pair-results.md`. Native query limits/gates are unchanged. Prototype-injected failures do not prove cleanup after an internal WASM exception; installed wrapper lacks finally for temporary native allocations, and no genuine internal failure or vendor patch is claimed.

Accepted dependency: **12b4a36**, detached constant-rate contact-motion compiler. Both SPEC findings (identity order and float32 underflow) and QUALITY findings (small-angle NLERP speed-bound mismatch and sparse navigation dimensions) are closed with retained RED→GREEN regressions. Final ordered SPEC PASS and QUALITY APPROVE; parent **21/21 focused,137/137 strict/native PASS59.081s, production build and staged whitespace PASS**. Full evidence: `2026-09-13-contact-motion-results.md`. No browser/visible spell acceptance belongs to this nonvisual compiler increment.

The active pair-query plan has independent numerical/SPEC PASS; exact guarded prune, zero-speed latch and bracket clauses are incorporated. Preserve the50µm empirical guard,1mm uncertainty and128-call total per pair, with a cross-span uncertainty latch and integrated travel. `2026-09-13-contact-ordering-design.md` is only a downstream working note, not another active implementation. See `2026-09-13-contact-3d-probe-results.md`.

Claude delta at 09:20 UTC: same environment HEAD `9f155d8`, but new untracked `docs/environment/restart-point.md` reports Claude paused on Simon's request at 11:17 local. Preserve his paused WIP; do not start his work or treat this document as a command to pause Codex. His browser run/review are explicitly interrupted, screenshots stale and final handoff absent. No direct Claude message delivery or response has been verified; current coordinator feedback is `docs/handoffs/claude-sync.md`.

Historical family publication: live sp1e identity, canonical-origin and both ancestor checks preceded the atomic push of **5d7e56d9a1370ebdf5f3f604b2e6951cbb626cd5**. This is superseded by accepted owner-batch **fcd186e** above; it is not the latest remote SHA. Current Slicer WIP, research cache, build output, exe and website deployment have not been published. The sections below are historical acceptance records, not a new instruction to stop after each task.

Latest read-only Claude WIP delta11:34UTC remains unchanged from11:14: paused9f155d8 and exact preserved files; no final `docs/environment/handoff.md` or `integration-requests.md` at the prior presence check. No direct message acknowledgment or restart. Do not edit/merge/restart its workstream.

Fresh12:04UTC read-only check reconfirms exact9f155d8HEAD/unchanged WIP, only the existing restart-point and no final handoff/integration-requests file. No acknowledgment, merge or restart. Parent's new `2026-09-13-authored-figure-plan.md` is explicitly a **draft**, not another production assignment: numeric skin/bridge/cap contract, owned paths and tooling need internal resolution/contract review after live-Slicer acceptance. Independent read-only numerical authoring proposal is underway; no asset generation/install.

`2026-09-13-owned-contact-batch-design.md` and exact plan now incorporate independent contract PASS: full stored topology, explicit pure/native allocation boundary, one retained wrapper per owner lifetime, no fake subset completeness, no same-tick replay budget reset, and queries inside each relevant successful fixed-step callback. A prior old-reviewer resume failed at a runtime limit; existing non-family SPEC was reused and a fresh final QUALITY reviewer was subsequently available. No user-facing tasks were created. Claude's subsequent read-only delta remains unchanged paused9f155d8/WIP with no final handoff; all its files are untouched.

## Latest accepted development checkpoint — 2026-09-13

Accepted inspector source/results are committed as **0d4d1a22558a238120bbe546bb0a93d9a43af095**, pushed and verified on both remote branches. A fresh owned Vite preview (exec session 78951) now serves port 5173, with HTTP 200 verified for both the normal yard and arm-lab HTML containing the new inspector. It is intentionally left available for Simon; verify liveness on return rather than assuming this session survives. Test-owned processes remain closed. This publication/preview record is a following docs-only checkpoint.

Native-history plan Tasks 1–5 are now complete at bounded arm-lab scope. Independent SPEC PASS and ordered QUALITY APPROVE; parent final **116/116 native PASS (99.470 s), strict typecheck/production build PASS, complete 14/14 browser PASS (218.596 s)**. Parent inspected both final history captures, current tick 190 and recorded samples 0/4, with the expanded panel fully visible at 1440×900. The renderer never restores physics from historical data. Mere paused blur preserves selection; live/resume/reset/handoff/context/pagehide clear it. Both the geometry-comparison assertion and sidebar-height regression are retained. See `docs/superpowers/plans/2026-09-13-native-motion-trace-results.md` for commands, controls, screenshots and material trace-copy cost limits.

Next: resume the existing `2026-09-08-moving-contact-next-slice.md` and preserved rotating/native-chord diagnostics, using the now-available measured trace. Write and review a bounded contact-query TDD plan before implementation. Do not infer continuous native trajectories or accepted general moving-contact accuracy from boundary samples. Full humanoid/gore/projectiles/chapter and hardware FPS remain unaccepted. Do not rebuild engines or expand into Claude's environment namespace.

Source is being checkpointed/published on `main` and `codex/vadstena-runtime-foundation` as explicitly requested; inspect actual HEAD/remote refs rather than treating older SHA/status paragraphs below as latest. The user-owned research cache stays untracked and untouched. No test-owned process remains after the passing suites. Local preview liveness is a separate check, not established by an ambient browser URL. No Windows executable or sp1e.se deployment was produced by this increment.

## Publication update — 2026-09-13

The GitHub blocker below is resolved. User created the public repository and explicitly authorized uploading the project. GitHub CLI now verifies as `sp1e`; Git's older credential helper still selected another account, so publication uses per-command `-c credential.helper= -c 'credential.helper=!gh auth git-credential'` without erasing or changing the other stored Git credentials. Remote `origin` is `https://github.com/sp1e/Stones-of-Vhat.git`. User's initial README commit was preserved via a merge; **d960d006548953419e8b0232bb2deba4e6eef792** was pushed and verified on both `main` and `codex/vadstena-runtime-foundation`. The pinned Claude base `20d9cf5` remains in that published ancestry. No environment branch/worktree was created by Codex; user is asking Claude to start it independently.

Independent bounded publication audit covered 98 tracked paths, 206 unique historical file blobs and 43 commits at `dc713bd`. No credential or third-party workplace-data blocker was identified; this is not a comprehensive security scan. Only tracked source/tests/project documentation and history were pushed, not the untracked research cache, dependencies, build outputs or exe. User authorization includes this project documentation; future commits still require scoped review before publication. Below are preserved earlier checkpoints, superseded by this update for GitHub status.

## 2026-09-13 checkpoint — supersedes older status below

User continues PC development and names the project **Stones of Vhat**. Existing package, executable and `/vadstena/` paths are intentionally unchanged. Core native trace is accepted/committed as `20d9cf5`; actual arm trace integration now passed independent SPEC and QUALITY gates plus coordinator strict/native **115/115 PASS, 52.924 s**. See the execution checkpoint in `docs/superpowers/plans/2026-09-08-native-motion-trace-plan.md`. Next implementation is Task 3 (pure historical frame adapter), then Task 4 paused inspector and Task 5 browser/visual gates. No full Slicer query, humanoid, gore or chapter acceptance.

User also requests a Claude Code parallel branch and public GitHub repository. The local handoff is `docs/handoffs/2026-09-13-claude-environment-workstream.md`: environment-only work from pinned base `20d9cf5c7578656f59bebfaf5b5594145971f1ff`, proposed branch `codex/environment-blockout`, separate worktree. Neither that worktree nor branch has been created by this handoff. Claude must never edit the runtime-foundation checkout. A public repo is not a prerequisite for this local collaboration.

GitHub creation currently blocked on the correct browser login: connector identity is `sp1e`, but it exposes no repository-creation operation; CLI active account is `Simon-HF` (inactive `simonpsson`) and was not switched. `sp1e/Stones-of-Vhat` returned 404. The in-app browser is on GitHub sign-in; user was asked to log in as `sp1e`. **No repo created, source pushed, Git remote changed or private history published.** Verify current state after login before creating; do not infer that a repo was created from this requested name. Public creation should start with empty/safe introductory content, not the unreviewed local history or this machine-specific handoff.

Task 2 is committed as **bf1910b** (arm fixture, new native-trace test and execution-plan checkpoint only). User-owned research `docs/research/deep-research/2026-09-08/work/` remains untouched and untracked. Coordinator documentation is committed separately; inspect actual HEAD. No Windows rebuild/launch, security or dependency change occurred. No server was restarted in this checkpoint; ambient localhost UI is not liveness evidence. Older browser screenshots/results below belong to the earlier arm baseline, not fresh native-inspector acceptance.

## Earlier accepted baseline (2026-09-08)

**Latest verified outcome:** Simon said "fortsätt" after the Grip delivery. B01a body-motion/pose-authority core is committed as **4bbb42c** and the accepted arm/adapter/dev lab as **925bda9**. Final independent SPEC PASS and QUALITY APPROVE, coordinator strict **104/104 native PASS**, web build PASS and fresh **13/13 browser PASS** (168.918 s). A supplementary dense diagnostic passed 759 distinct handoff phases /3,643,200 intermediate native samples. The arm uses the explicitly reviewed **eight native 1/480 physical steps inside one authoritative 1/60 interval**; animation/bootstrap and normal yard retain their original stepping. The earlier one-step and four-step failures remain documented, not erased. No full humanoid, anatomy/gore, projectile or chapter acceptance is claimed.

Simon explicitly resumed after compact, supplied the completed research and authorized autonomous PC development while away. The old pause snapshot is superseded (preserved in Git at `b4c6a06`). Research supports development; it is not a reason to wait for another session. No new design interview or routine approval gate is needed for the unchanged approved scope.

## Exact workspace

- Worktree: `C:/Users/simon.pettersson/.config/superpowers/worktrees/Game1/runtime-foundation`.
- Branch: `codex/vadstena-runtime-foundation`; game in `game/`. The configured original Game1 cwd is not this implementation worktree; verify Git root before edits.
- Every shell command starts `rtk`. Use `rtk proxy` for full output and wrap PowerShell cmdlets. All local file edits use `apply_patch`.
- Relevant commits: `05dbc96` research integration/adoption; `1bb5bfe` Grip core; `05aeb5e` relative-contact diagnostic; `ad80e58` playable Grip; **`4bbb42c` motion/pose core**, `5047564` original arm plan/controls, **`925bda9` accepted arm/adapter/lab + results/addendum**. A following coordinator documentation commit refreshes this handoff/README/next brief; inspect actual HEAD. Keep this existing long-lived worktree/branch; no merge, deletion or push is requested.
- Only `docs/research/deep-research/2026-09-08/work/` remains intentionally user-owned and untracked. The ten final reports are now tracked, unchanged. Do not stage/delete/rewrite the work/source cache.

## What is actually complete

- M0/M1A runtime and PC courtyard: Three 0.185.1, Rapier 0.20.0, TypeScript 7.0.2, Vite 8.2.2; fixed 1/60 simulation, capsule movement, stairs/ramp/props, pointer lock/pause, independent gore preference.
- **M1B-1 ordinary-prop Grip**: real dynamic bodies with bounded linear/angular impulses; mass/range/sight/shape/contact guards; lift, wheel distance, R+mouse rotation, physical placement/release and one-shot throw. Sparse HUD, held highlight and visible gold tether. Default RMB hold; optional clearly labeled session-only toggle.
- **B01a motion/pose core**: epoch/body/collider identity, detached completed intervals, explicit origin/COM distinction, nonidentity rigid bone/body binds, sampled COM/world angular velocity and in-place all-member animation-to-physics authority transfer.
- **B01a arm/adapter/lab**: two persistent bound segments, actual offset COMs and native joint limits, fixed floor/wall, one-shot off-COM impulse, eight measured physical native substeps per outer tick, pause/reset/fatal teardown, and separate dev-only visual view. Four bodies/four colliders/one joint and 3 kg arm mass remain stable; no anatomical cone claim.
- Final strict **104/104 native PASS** (19.472 s), fresh **13/13 browser PASS** (168.918 s), web build PASS. Nine original browser cases remain, including actual full-Chromium BFCache, production diagnostics stripping and ten acquired-Grip restarts; four lab cases add actual handoff/contact, ten resets, graphics recovery and production exclusion. Parent opened all six regenerated final lab PNGs, including before/after tick 48 and contact tick 184.
- Final independent spec and quality approvals complete. Quality's supplementary dense 1–755 and four longer-phase probes passed: worst anchor **0.519445 mm** and penetration **0.676288 mm** against 5 mm gates, projected X/Y <=0.40 rad, speed <=12 m/s, omega <=50 rad/s and energy within its declared allowance. Those diagnostic samples are not extra normal test cases or universal phase/full-body proof. Existing large-bundle advisory and missing hardware FPS acceptance remain.
- Gore remains **ON on first start**, saved false persists. This is the preference; NPCs, blood/limb effects and final gore presentation are not implemented.

## Fixes and semantics that must survive

- Input owns physical button-edge suppression. `GripCommand.wanted` is a level, but `acquire` and `throwPressed` are consumed fixed-step event pulses. Distinct adjacent true samples may be separate clicks: do not add a second level debounce in physics. Real input-to-Rapier hold/toggle regression covers the previously discarded next click after a miss.
- Rotation/throw uses confirmed held state, not desired grip. Failed pickup/throw clears intent without consuming R camera look or requiring an extra toggle click. Pre-acquisition LMB cannot queue a delayed throw. Preserve pending acquire until its first physics sample.
- Pause releases immediately without another physics step; focus/pointer-lock loss, restart and disposal clear pending input. No prop teleport/velocity overwrite/kinematic conversion, persistent force accumulation or hidden grip joint.
- Actual solver contacts, not broad-phase pairs, prove the plank contacts both walls. Core tests cover sleeping barrel, mass comparisons/caps, energy bounds and fixed-command 30/60/144 Hz grouping.
- Full Chromium is required for the real BFCache regression. Generated desktop/release/.playtest paths stay excluded from the Vite watcher.
- Tether origin is forward from the camera plane; its initial coplanar version was invisible. Reuse/dispose its geometry/material and warm it before comparing GPU counts.
- Native-body identity is exact world object membership, not numeric handle containment. Public refs carry a fresh UUID world epoch, body ID and collider ID; no handles escape. Registration and returned records are detached; stale worlds/aliases reject. Shape and authority changes are explicit interval discontinuities.
- COM velocity must use offset world COMs, not body-origin displacement. Rigid transforms reject scale and invalid quaternions; normalize robustly, including native float32 comparison. Preflight all-member handoff and derived float32 responses before any setter; unexpected native failure/poison invalidates the authority and owning world. Preserve position-kinematic-only and no-queued-target boundary guards.
- The arm's outer waveform/frame is sampled once; divide its torque impulse into eight equal opposite pairs, measuring work before each native contact solve. Point impulse occurs once. Capture/publish only after the entire batch succeeds; a failed inner setter/step/measurement must leave no live half-completed world.
- `lastPhysicsStep` contains detached per-native metrics and contemporaneous energy allowance, not full collider-pose history. Endpoint metrics remain endpoint metrics. `wallTicks` counts outer contact ticks, `wallNativeSteps` counts inner contact steps and `wallImpulseNs` sums native impulses once. Core owner comments describe authoritative batches; normal-yard behavior is unchanged.
- Preserve original good/bad solver controls, the four-quarter pair and phase-264 regression. The four-step candidate passed 103/13 normal gates but failed the dense 5 mm bound at 5.882782 mm; only the eight-step fallback received final acceptance. Do not simplify back to four based on earlier green tests.

## Next unambiguous development slice

Read [B02a moving-contact brief](docs/superpowers/plans/2026-09-08-moving-contact-next-slice.md), then write and fully review its exact executable TDD plan before production edits. **The brief exists; no B02a projectile/contact module has been implemented.** Continue directly within approved scope, without reopening the design interview.

Prove time-aware Slicer volume contact against the moving arm, a nearer moving shield/prop, a thin wall and a farther body. Preserve relative trajectories, common TOI units, closest blocking order, stable cast/contact identities, detached finite contact frames and matched close misses. Explicitly bound rotation/interpolation/float32 coordinate precision and handle initial overlap/discontinuities. The current arm metrics do not supply missing native-step collider trajectories; any needed history extension must be explicit and reviewed. The saved analytical crossing is a seed, not proof of angular CCD.

Full B01 hip/knee/full-body stairs/wall/anatomical/24-body budget, wider B02 damage/zone ordering, B03 skin/caps/nine prepared separations and B07 actual GLB/KTX2/audio in both exports remain open before B04 engine lock. Focus, playable ranged magic, NPCs, melee, gore graphics, historically phased environment and full chapter follow. Do not silently change engines or treat a small arm as full M1B completion.

## Vision and historical constraints

Approved 15–20 minute first-person mystical Vadstena chapter, 1510 plus explicit time fractures, Rådhus–Storgatan–S:t Per route, medieval weapons/magic, Tyngd/Klang first, six adult NPCs and telegraphed hostility, physical and sound solutions, two school choices and fail-forward. All five landmarks remain in the wider vision. Full spec governs nine prepared sever connections; no arbitrary mesh slicing promise.

Follow [research adoption](docs/superpowers/plans/2026-09-08-research-adoption.md): S:t Per is a whole medieval church, not merely today's freestanding Rödtornet; modern plazas/roofs and the post-1510 castle must not masquerade as baseline history. Building-phase uncertainty and rights/provenance stay explicit. The current yard is only test geometry.

## Windows, preview and scope boundaries

- Earlier accepted unpacked exe: `game/release/win-unpacked/Vadstena.exe`, recorded SHA256 `386e91ab57d7162c5809a2e681ce1185891083462d3d5ed506ab5e03c27c6d2d`. **That build contains neither Grip nor B01a.** No desktop rebuild or exe launch occurred during these increments.
- Prior September 8 controlled testing, explicitly approved by Simon: two consecutive 2/2 desktop passes, AMD Radeon 860M/D3D11, real start/muslås/movement/pause and isolated-profile persistence. Portable artifact, ordinary/default-profile launch, future packaged assets and FPS remain unverified.
- Defender previously quarantined a generated exe as Trojan:Win32/Cinjo.O!cl; Simon allowed it himself. Provenance and no new matching event during prior controlled runs do not prove a false positive or safety. No assistant changes to Defender/exclusions/restoration; stop on renewed warning.
- Local Vite preview restarted at `http://127.0.0.1:5173/vadstena/` (owned execution session **3553**); both yard and `/vadstena/arm-lab.html` freshly returned HTTP 200. Intentionally left running; verify liveness on return. All test-owned browsers/servers are closed.
- PC keyboard/mouse only; dedicated mobile work deferred. No public deployment, website change, purchase, dependency update or security-setting mutation. Preserve unrelated/user work.

## Selective loading / team contract

Use `codex-team-workflow`, test-first implementation and ordered independent spec then quality review. Native agents only, one coupled production writer; read-only research/reviews may run alongside it. Coordinator owns team-state/handoff. Do not reopen settled user choices.

Start with `game/README.md`, this file and `docs/superpowers/plans/2026-09-08-moving-contact-next-slice.md`; then read relevant approved spec/research/installed API portions. Current detailed evidence is `2026-09-08-body-motion-results.md` and `2026-09-08-arm-lab-results.md` under `docs/superpowers/plans/`; the latter includes exact dense-probe reproduction and failed candidates. Core/arm original plan snippets are historical and carry execution/addendum banners; never blindly replay them over accepted code. All current workers have concluded their code/review tasks; no pending code writer needs resuming. The runtime rejected fresh review spawning, so independent existing reviewer roles were reused without production ownership. Dated team-state entries are historical unless superseded by the final closeout.

Reusable skill: `C:/Users/simon.pettersson/.codex/skills/vadstena-runtime-verification/SKILL.md` (load only relevant Rapier/Windows reference). Prior user-requested memory note is `C:/Users/simon.pettersson/.codex/memories/extensions/ad_hoc/notes/2026-09-08T152400-vadstena-windows-grip-pause.md`; its paused state is now superseded by this verified Git handoff. No personal-memory update was requested/performed during the current research/dev turn.
