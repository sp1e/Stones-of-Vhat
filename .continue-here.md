---
project: Game1 / Vadstena
phase: M1B-1 Grip
status: resumed_with_research
updated: 2026-09-08
---

# Resume after compact

**Superseding continuation:** Simon explicitly resumed and asked to integrate the completed 2026-09-08 research before continuing development. All ten final reports were read; see `docs/superpowers/plans/2026-09-08-research-adoption.md`. Grip plan revision is active. The paused snapshot below is historical; inspect current Git and latest team-state before acting. No new Grip implementation is claimed by this banner.

User explicitly paused development to save skills/memory/knowledge and compact. Do not resume merely because this file is read; wait for the user's continuation. Research runs in a separate user-owned session **in parallel** and is not a development gate.

## Exact workspace and ownership

- Worktree: `C:/Users/simon.pettersson/.config/superpowers/worktrees/Game1/runtime-foundation`.
- Branch: `codex/vadstena-runtime-foundation`; existing linked worktree, not a new one.
- Game directory: `game/`. All shell commands start `rtk`; use `rtk proxy` when full errors/results matter. File edits use `apply_patch`.
- Last completed development commit before this pause save: `607fc0a` — Windows acceptance fixes/results and the research handover. Subsequent pause commit contains this state and the partial Grip plan; inspect actual HEAD rather than assuming 607fc0a remains latest.
- `docs/research/deep-research/` is untracked work belonging to the parallel research session. Do not stage, move, delete, overwrite or infer completion from its source cache. Only consume actual findings when available.
- Paths originally supplied directly under `C:/Users/simon.pettersson/Claude/` were absent; the real handover/exe were in this worktree.

## What is genuinely complete

- Approved medieval Vadstena design/spec and separate worktree. PC only; mobile deferred. Both PC web and Windows exe remain goals.
- M0 and M1A browser physics courtyard: Three 0.185.1, Rapier 0.20.0, TypeScript 7.0.2, Vite 8.2.2. Fixed 1/60 simulation, capsule walk/sprint/jump/crouch, stairs/ramp/props, menu/pause/pointer lock, gore preference true by default with saved false retained.
- Fresh pre-Grip baseline 2026-09-08: strict types + **32/32 native tests**, **6/6 browser tests**. Browser suite took ~101 seconds with ~74 seconds in movement/ten restarts. Output was buffered; no failure. Actual BFCache regression remains included.
- Existing unpacked Electron 44.2.0 exe tested with explicit user approval: **2/2 desktop tests twice consecutively**, AMD Radeon 860M/D3D11, actual pointer lock, W movement, Escape pause, new-profile gore true and saved false after full process restart. No new matching Defender records during the runs or final check; no remaining target processes at that checkpoint.
- Tests corrected, not the game binary: firstWindow/reload race; debugger unsafe-eval bypass; error observation through gameplay and relaunch. Independent review approved.
- Executable hash stayed `386e91ab57d7162c5809a2e681ce1185891083462d3d5ed506ab5e03c27c6d2d`, 246202368 bytes. ASAR shell/renderer matched inputs. These are provenance and scoped acceptance, not security clearance.

## What remains incomplete

- Windows single-file portable artifact has not been built/accepted. Ordinary launch/default `%APPDATA%/Vadstena`, no-test-flag launch and FPS are unverified. User approved controlled testing, not security-setting changes; renewed warnings require stopping.
- M1B gameplay: **no Grip production code or new Grip tests exist at pause**, and no Grip RED/GREEN run has occurred. Only a partial executable plan exists. Do not mistake code blocks in that plan for implemented source.
- Magiprojectiles Tryckstöt/Skärklang follow Grip. Then M2 ragdoll/anatomy/gore, M3 historical environment, M4 chapter, M5 complete acceptance. No full chapter/publication claim.

## Work interrupted and exact next step

Agent `grip_implementation` (Astra/high, fresh bounded context, sole production writer) was interrupted on user pause. It had written `docs/superpowers/plans/2026-09-08-grip-telekinesis.md`: Tasks 1–3 (physics controller/yard integration/safety tests/pure input), roughly 560 lines at pause. Renderer/main/HTML/browser sections are not yet written. No active test runner matching Grip/browser was found during pause inventory.

Coordinator read the core plan and internally approved physics Tasks 1–2 for TDD. This internal approval does not override the current user pause. Required revisions before completing input/UI plan:

1. `gripInput.motion` currently checks requested grip (`wanted`) rather than actual held state. Failed pickup, throw or auto-release can incorrectly consume R+mouse and freeze camera look. Feed authoritative holding result back after simulation steps; gate rotation on actual hold and reconcile toggle intent after failures/throw. Test failed pickup + R/mouse look and toggle throw + next-click reacquisition. Do not clear a pending acquisition before its first simulation step.
2. Add explicit settled barrel pickup, plank rotation against wall/corner, break-range and reason-specific lost-sight tests. Existing free-plank rotation/box-wall tests alone do not cover these.
3. Complete exact renderer/main/HTML/browser integration plan, including real input automation, screenshots, pause/blur/restart clearing, detached readonly DEV diagnostics and resource lifecycle checks. Main must read and approve the remaining plan; no further user design approval required for unchanged scope.
4. Write tests and observe behavioral RED before production source edits. Implement one owner at a time, then separate spec review followed by quality review, actual browser screenshots and all regressions. Do not launch/rebuild desktop while implementing the browser Grip slice.

Accepted Grip-slice choices: real dynamic bodies, bounded linear/angular impulses, actual-shape obstacle checks, right hold/default with session-only labeled toggle setting, left-click throw edge, R+mouse rotation without camera motion, wheel distance. Keep gore codec untouched. Shared Focus and magiprojectiles can follow; Grip alone does not complete M1B. Preserve sparse HUD and prototype geometry; no redesign or dependency upgrade.

## Knowledge to load selectively

- Current state: `docs/superpowers/plans/team-state.md` (dated sections supersede older claims).
- Full approved requirements: `docs/superpowers/specs/2026-09-05-vadstena-chapter-design.md`.
- Windows evidence: `docs/superpowers/plans/2026-09-05-windows-portable-results.md`.
- Research brief: `docs/research/2026-09-08-deep-research-handover.md`.
- New reusable skill: `C:/Users/simon.pettersson/.codex/skills/vadstena-runtime-verification/SKILL.md`; read its Windows or Rapier reference only when needed.
- User-requested memory update note: `C:/Users/simon.pettersson/.codex/memories/extensions/ad_hoc/notes/2026-09-08T152400-vadstena-windows-grip-pause.md`. Registry MEMORY.md has older Windows-unlaunched status; the new note supersedes it. The registry itself was not edited.

## Resume contract

On continuation: inspect Git status and this file, preserve parallel research, finish/review the partial Grip plan, then continue TDD implementation. Use `codex-team-workflow`, TDD and ordered subagent reviews; native agents, no new sidebar tasks. No need to repeat the historical design interview or Windows preflight unless working on those changed inputs.

Compact has not been invoked by the agent: no explicit compaction tool is available. This file is the durable restart point regardless of whether context is subsequently compacted.
