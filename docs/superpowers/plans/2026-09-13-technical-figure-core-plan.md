# Technical figure core — execution plan for contract review

> Use test-driven-development and subagent-driven-development: one production writer, observed RED→GREEN, independent SPEC then fresh QUALITY. Parent owns coordination/review/results/Git. **Await independent contract PASS before assigning the writer.** User direction is already approved; this is an internal technical check, not another user approval checkpoint.

**Contract gate satisfied, 2026-09-13:** fresh independent Astra/high `figure_contract_spec` returned final PASS after six concrete findings were corrected. Corrections cover constant-channel exporter flags, exact angle/key construction, raw format allowlist, measurable fold/intersection scope with visual pinching deferred, animation time rebasing and optional skin-root export. Outstanding/consumed preparation lifetimes are explicit too. The sole 14-path writer may now begin. This is not implementation or asset acceptance.

## Outcome and exact boundary

From accepted/published `6186a087b819e8f4a04c7f2f7161a1928fbf67d5`, create the first editable project-authored adult technical figure, actual GLB export, bounded raw-data contract validator and engine-native two-instance/pose-preparation proof. Prove a real animated clothed elbow can retain its exterior and become two closed prepared surfaces without changing original inverse binds or a second actor. No new browser page, HUD, final art, physical ragdoll, damage authorization, blood system, sword, player locomotion change or historical costume claim in this core.

This is the first executable subset of the earlier broad `2026-09-13-authored-figure-plan.md`. Its old disappearing-bridge recipe is superseded by the numeric contract and final seam-options note. Do not widen the work into a model-viewer project. A small actual browser rendering/visual acceptance pass follows this core, then the anatomical owner/contact transaction; the full user combat requirements remain mandatory.

Read completely: `2026-09-13-technical-figure-contract.md`, installed-export inventory, final elbow-seam options, anatomy-next-slice brief, approved chapter and combat addendum. Research backlog B01–B03/B07 and technical dossier sections 3–7 are evidence, not new instructions or completed tests. Use the installed Three/Blender interfaces; read relevant asset-pipeline and Three game skill references before implementation. No repeat of the full historical research.

## Sole writer: exact owned paths

New source/generated asset:

1. `game/assets/source/technical-figure/authoring.json`.
2. `game/assets/source/technical-figure/figure.blend` — generated editable source, not a hand-authored binary patch.
3. `game/assets/source/technical-figure/README.md` — asset-specific provenance/export commands, not shared project status.
4. `game/src/assets/technical-figure.glb` — generated processed output; not in public/ and not imported by the production game.

New code/tests:

5. `game/src/assets/technicalFigureContract.ts`.
6. `game/src/assets/technicalFigure.ts`.
7. `game/scripts/author-technical-figure.py`.
8. `game/scripts/process-technical-figure.mjs`.
9. `game/scripts/verify-technical-figure.mjs`.
10. `game/tests/technicalFigureContract.test.mjs`.
11. `game/tests/technicalFigure.test.mjs`.

Narrow existing tooling changes:

12. `game/package.json`.
13. `game/package-lock.json` — generated npm lock delta.
14. `game/tests/browserFoundation.test.mjs` — only exact dev-tool/script assertions, retaining current runtime versions and all existing checks.

The writer may generate intermediates/reports only under existing ignored `game/.cache/technical-figure/`; do not modify the ignore file or user cache. Everything else, including all physics, arm/Slicer, normal game, browser tests, Vite config, desktop/security, Claude environment and coordinator docs is read-only. No worker Git writes. On a concrete dependency/interface defect, report it instead of patching an unowned file. No nested writers.

## Task 1 — source contract and failing negative tests

Encode the numeric contract as versioned authoring JSON with explicit bone formulas/data, units, LOD/unit inventory, cap mappings, clip and budgets. Values are fixed by the companion contract; do not fill them with hidden arbitrary tuning. Python authoring consumes this file. TypeScript tests independently reconstruct selected basis/rest/clip expectations instead of trusting the generated manifest to validate itself.

`technicalFigureContract.ts` exports a bounded raw-GLB reader/validator accepting a Uint8Array and returning detached verified descriptions/accessor data. It must validate original bytes **before GLTFLoader can normalize malformed skin weights**. No Rapier, network, DOM, filesystem or dev-tool import in this runtime module. It may use installed Three math, but not use its loader as a replacement for raw checks.

Limit input to 2 MiB; valid GLB2 header/declared length/chunk bounds, one JSON and one embedded BIN, supported finite accessors with checked byte offset/stride/count arithmetic, valid indices and node hierarchy with no cycles/duplicate parents. Only the agreed uncompressed, resource-free TRIANGLES subset is accepted; reject external URI, sparse/compressed/unsupported accessor features rather than silently misread them. No allocation or traversal based on an unchecked enormous count. Error text names the violated contract.

The companion contract section 2a is the exact supported format allowlist, not an invitation to invent a general glTF parser. Implement its attribute component/type/normalization, stride/alignment, node/scene/TRS, morph/extension/resource, counts and material restrictions and retain malformed-byte controls for each category. Section 5 fixes semantic-key formulas and allowed adjacency independently of extras. Unsupported actual exporter output is a reported interface issue requiring a narrow reviewed correction, never an implicit broadening of the parser.

Check actual rest transforms/inverse binds, one 11-joint skin, both LODs/42 primitives, stable IDs/roles, vertex-key mapping, original weight sums/allowlists, position/normal/UV finiteness, indices, winding/semantic closure and the actual animation. Do not trust only extras that say `closed` or `physicsAccepted:false`; verify geometry, and reject accidental physics fields rather than interpreting them. Exact Three-ready mapping is built after raw acceptance.

Observe missing-contract/asset RED, implement the smallest useful validator behavior, then retain actual malformed-data controls. At minimum: wrong/duplicate joint or unit ID, changed basis/rest bind, missing LOD cap, foreign/cross-bulk weight, wrong joint index, bad weight sum that Three would repair, missing/conflicting `_SVH_VERTEX`, bad index or accessor bounds/nonfinite component, negative/nonuniform scale, open cap edge, wrong winding, missing/extra clip channel and excessive file/count budget. Mutate actual JSON/BIN values or construct explicit malformed fixtures; no test that merely searches source strings for compliance. Do not weaken the exact dependency guard while adding the selected tools.

## Task 2 — local pinned export/processing chain

Add only exact **devDependencies** `@gltf-transform/core:4.5.0`, `@gltf-transform/functions:4.5.0`, `gltf-validator:2.0.0-dev.3.10`. Runtime dependencies remain Rapier 0.20.0 and Three 0.185.1. Add exactly `asset:figure:process` and `asset:figure:verify` npm scripts calling the two corresponding retained Node scripts. Blender authoring remains an explicit command because its verified binary path is machine-local. Update exact foundation assertions, not a broad permissive comparison.

Patch the manifest with apply_patch, then use a normal local npm install with `--ignore-scripts` to generate/install the declared lock graph without rerunning Electron postinstall. Do not install globally, run arbitrary npx latest, use force/audit-fix or change shared config. Inspect the lock delta, resolved direct versions and actual installed helper source/types before use. A required install-script/native dependency problem is a concrete issue to report, not permission to bypass security or silently change versions. Registry integrity checks are availability/provenance, not a security audit.

Use the verified `C:/Program Files/Blender Foundation/Blender 5.2/blender.exe` with `--background --factory-startup --python scripts/author-technical-figure.py -- ...`. Recheck version before the first export. The generator must resolve and constrain its source/output paths to this named asset and ignored artifact directory; never clear/reset a broad directory. It writes editable `.blend`, raw `.cache/technical-figure/raw.glb` and a bounded source-audit report. No GUI/preferences, external textures/models or user file reuse. Authoring code/config uses apply_patch; binary assets are generated by the retained pipeline.

Use the inventory's explicit exporter settings, with the deliberate contract change `export_attributes=True` for `_SVH_VERTEX`. Only owned supported POINT/FLOAT metadata attributes; actual exported attribute preservation is a gate. Keep root/bones intact, rest-position armature, single active proof action, sampling every frame, no export compression, hierarchy flattening, leaf/helper rig or current-pose inverse-bind rewrite. Verify source weights before the exporter can truncate/normalize. Every vertex must be assigned to the intended rig so an accidental neutral bone does not appear.

Also explicitly set `export_optimize_animation_keep_anim_armature=False` and `export_optimize_animation_keep_anim_object=False`, with animation optimization still off. Source-read evidence shows the inherited true armature default would retain constant TRS for all bones. Require the exact single forearm-R rotation channel and 121 LINEAR samples/angle construction in contract section 2; do not add a postprocessing channel-prune pass to conceal a different export.

Set source `fps=60`, `fps_base=1` and `export_anim_slide_to_zero=True` explicitly: source frames 1..121 must become glTF time 0..2, not the default 1/60..121/60. The installed exporter omits optional `skin.skeleton`; accept absence while deriving/checking the exact pelvis/common-root hierarchy from nodes, and validate the exact reference if present. Retain wrong root and shifted-time negative controls without injecting optional metadata to make the asset pass.

`process-technical-figure.mjs` reads the raw GLB, runs Khronos validation and project raw contract, then uses the actual installed glTF Transform `NodeIO` and **`dedup({ propertyTypes: [PropertyType.ACCESSOR] })` only**. No mesh/material/node join, flatten, prune, simplify, resample, quantize or decoder. This selectively shares duplicate buffer data while retaining anatomical objects and clip evidence. Independently compare named unit/skin/clip/key/accessor semantics before/after; validate the processed file again before replacing only the named final GLB. Record raw/output hashes, byte sizes, counts, versions and Khronos issues. Treat errors as failure, explain rather than hide warnings.

Official API basis: [NodeIO scripting overview](https://gltf-transform.dev/), [dedup](https://gltf-transform.dev/modules/functions/functions/dedup), [DedupOptions](https://gltf-transform.dev/modules/functions/interfaces/DedupOptions). Parent verified the current documented selectable property type; the actual installed pinned API still needs inspection. No upload to an online model service or global CLI is needed.

## Task 3 — actual Three asset and prepared visual ownership

`createTechnicalFigureAsset(bytes)` first runs raw validation, then parses through installed `GLTFLoader.parseAsync` with no external resources. Do not normalize first and retroactively call the raw file valid. Verify loaded joint/unit/clip associations against the verified source, initialize hidden cap/LOD state off-scene, and keep the canonical hierarchy immutable. If post-load preparation fails, dispose the resources actually acquired. Do not claim to cancel/dispose inaccessible internals of a rejected Three parser.

Return an asset owner with `createInstance()` and idempotent `destroy()`. Track its instances; destroying an instance releases its own skeletons/materials/replacement buffers, not shared canonical geometry. Destroying the asset disposes live instances first and then canonical resources exactly once. `SkeletonUtils.clone` must clone the common root containing all bones; original rest inverse matrices are shared immutable data unless deliberately deep-copied. Never calculate inverses from the preview pose.

Track uncommitted preparations under their instance as well. Instance destruction disposes them; a successful commit transfers their buffers to the live instance and makes later prepared-object disposal harmless to those buffers. Retain both ownership regression cases and competing pending preparations.

Each instance exposes its render root plus explicit preview controls: `setTime(seconds)` for the bounded real clip, `setLod(0|1)`, `prepareElbowPreview()`, `commitElbowPreview(prepared)`, `read()` detached diagnostics and `destroy()`. A prepared object is bound to that instance, asset revision/generation and pose version, single-use and explicitly disposable. Commit after any pose change or against another instance rejects without partial visibility/buffer mutation. Prepare both LODs off-scene with the exact formula in the contract; original template/bulk geometry is unchanged. No damage/contact/physics authority is exposed by these methods.

For the opened preview, freeze animation at the cut pose; reject further `setTime` until a new instance/reset rather than regrowing or continuing cross-component animation. LOD may switch without restoring intact surfaces. Shared original inverse binds and canonical buffers remain bit-for-bit unchanged. Both cap sides must close their own retained surface. Use conservative bounds over the declared clip for intact meshes and the actual baked component positions for replacements; never rely on a cached rest sphere to prove a bent part remains visible. GPU rendering/culling acceptance follows later.

Retain native tests using the real exported GLB, actual GLTFLoader, actual AnimationMixer/SkeletonUtils/SkinnedMesh vertex transformation and independently reconstructed matrices. Two clones: animate/open A, leave B's bones/geometry/visibility/materials untouched. Check real mixed-weight deformation, original world exterior preservation, seam/cap closure, owner-only replacement weights, no topology generation, both LODs, stale/foreign/duplicate preparation, preparation/disposal failure leaving intact state, repeated instance lifecycle and canonical release. Instrument resource disposal events with restoration in finally; do not call that a hardware GPU-memory proof.

## Task 4 — retained asset audit and core acceptance

`verify-technical-figure.mjs` audits actual raw and processed files, all 121 samples at both LODs and actual Three preparation/closure. Apply the fixed budget/tolerance rules, report extrema and any untested geometric relation. Bound geometry work to the authored elbow envelope and declared semantic adjacency; other intentional technical-joint overlaps cannot excuse an elbow error. No unbounded brute-force loop or timed wait that assumes completion. Data validation that cannot establish a required relation must fail or explicitly leave that gate open, not assert manifest truth.

The audit must enforce the specified area floor, moving-reference orientation dot, directed closure and exact 36/78 unit-pair intersection inventory/tolerance from contract sections 5 and 7, with negative fixtures for shared-edge overlap and forbidden skin/cloth/cap intersections. Perceptual pinching and natural appearance remain explicitly unverified until the next browser visual gate; do not claim these from area/closure alone. Preserve the exact failing geometry/pose if a numerical predicate fails. Propose a narrow numeric/source correction for internal review; do not produce a dishonest green by dropping the 100 degree sample, relaxing normals/weld, deleting exterior or adding separation impulses.

Document in the asset README: project-authored provenance, actual source/export/tool versions, hashes and reproducible commands, technical appearance/geometry limitations, no invented asset licence or historical claim. This is core numerical/export acceptance only; actual lit/shadowed browser close/far views, full physical cuts and graphic/neutral gore remain separate next gates. Do not produce a screenshot or video that labels a manual preview as combat.

Writer runs focused tests, strict checking, asset process/verify, production build/exclusion and whitespace, then freezes its 14 paths. Independent implementation SPEC must pass before a fresh QUALITY reviewer. Parent reads final sources, lock delta and raw/output/audit evidence; reruns strict/native/build and the complete browser regressions sequentially with any same-output build. No source/model is accepted while required geometry gates are open. Parent alone stages exact paths and publishes after fresh identity/remote/ancestry checks. User research cache, raw outputs, installed dependencies and captures stay excluded.

## Immediate next gameplay step

After core acceptance, a bounded Three browser view proves real rendering, lit caps, both LODs/clones and warm lifecycle/culling; it does not become an indefinite viewer-polish task. Then create the explicit anatomical owner/graph revisions and consume a unique current accepted Slicer contact at a prepared zone, preserving existing body identity/mass/pose/velocity, removing only the right joint and publishing the prepared visual delta atomically. Keep ambiguous-contact and wrong-zone/angle negatives. Full B01 hip/knee/stairs, all nine cuts/second loose-chain cut, player/NPC capability damage, sword and default-on blood remain mandatory. Existing fixed four-body owner batch must not be weakened to pretend it owns a changed graph.
